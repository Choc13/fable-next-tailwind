{
  "name": "fable-next-tailwind",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "dotnet fable watch -s -o dist --run next dev dist",
    "build": "dotnet fable -o dist --run next build dist",
    "start": "next start"
  },
  "dependencies": {
    "next": "^11.0.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.3.3",
    "autoprefixer": "^10.0.4",
    "postcss": "^8.1.10",
    "tailwindcss": "^2.1.1"
  }
}
